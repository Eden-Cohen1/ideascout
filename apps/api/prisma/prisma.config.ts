// Prisma config placeholder — activated in the data-model milestone (when Prisma
// is installed). With Prisma 7 this is where the datasource URL and seed command
// are wired, e.g.:
//
//   import { defineConfig } from 'prisma/config';
//   export default defineConfig({
//     schema: 'prisma/schema.prisma',
//     migrations: { seed: 'ts-node prisma/seed.ts' },
//   });
//
// Left inert for now to keep the scaffold dependency-free.
export {};
