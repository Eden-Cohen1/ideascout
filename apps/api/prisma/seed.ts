// Database seed — implemented in the data-model milestone.
// Will create a demo user + sample project/idea so a fresh DB is immediately usable.
async function seed(): Promise<void> {
  console.log('seed placeholder — implemented in the data-model milestone');
}

void seed();
