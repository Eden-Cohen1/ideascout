# ideascout Redis config (BullMQ backing store).
# AOF persistence so queued/in-flight jobs survive restarts.
appendonly yes
appendfsync everysec

# BullMQ requires keyspace eviction to be disabled (no maxmemory eviction of job keys).
maxmemory-policy noeviction
